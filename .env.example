# Copy to .env (or pass via --env to `claude mcp add`). Do NOT commit real keys.
INSIGHTLY_API_KEY=your_insightly_api_key
INSIGHTLY_POD=na1
# INSIGHTLY_READONLY=1   # uncomment to disable all writes (safe mode)
