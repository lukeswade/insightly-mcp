# Never commit credentials
.env
*.key
keys.json

# Python
__pycache__/
*.pyc
.venv/

# OS/editor
.DS_Store
.idea/
.vscode/

# injected at build time; must never be committed (public repo)
bridge/server/_secret.py

# bundles carry the endpoint credential; they are released to R2, never committed
bridge/dist/

# rendered with the live install link; never committed
docs/artifact/build/
